model:
  name: shiftfuse_zero_nano_multi
  variant: nano_multi
  num_classes: 60
  use_se: false
  use_k3_adj: false        # replaced by multi_gcn per-partition GCN
  multi_gcn: true          # K=3 per-partition: separate W per structural group
  use_adyn: false
  drop_path_rate: 0.05
  dropout: 0.05
