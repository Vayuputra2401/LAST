%%% ACM MM 2026 – BNI Track
%%% Anatomy is Free: Zero-Parameter Structural Priors
%%% for Efficient Skeleton Action Recognition
%%% Authors: Pathikreet Chowdhury, Anubhav Bhattacharya, Dr. Gargi Srivastava

\documentclass[sigconf,authordraft,anonymous]{acmart}

%% ── packages ──────────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{amsmath,bm}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{microtype}
\usepackage{standalone}  % compile TikZ figures inline
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{shapes.geometric,arrows.meta,positioning,fit,calc,decorations.pathreplacing,backgrounds,patterns}

%% ── colours ───────────────────────────────────────────────────────────────
\definecolor{bestcol}{HTML}{1565C0}
\definecolor{freegreen}{HTML}{2E7D32}
\definecolor{learnblue}{HTML}{1565C0}
\definecolor{fusred}{HTML}{C62828}
\newcommand{\best}[1]{{\color{bestcol}\textbf{#1}}}
\newcommand{\ie}{\emph{i.e.}}
\newcommand{\eg}{\emph{e.g.}}
\newcommand{\etal}{\emph{et al.}}

%% ── ACM metadata ──────────────────────────────────────────────────────────
\setcopyright{acmcopyright}
\acmConference[MM '26]{ACM Multimedia}{October 2026}{Melbourne, Australia}
\acmBooktitle{Proceedings of the 34th ACM International Conference on Multimedia}
\acmDOI{}
\acmISBN{}

%%% =========================================================================
\begin{document}

\title{Anatomy is Free: Zero-Parameter Structural Priors\\
       for Efficient Skeleton Action Recognition}

\author{Pathikreet Chowdhury}
\affiliation{\institution{Institution}\country{India}}
\email{pathikreet@institution.edu}

\author{Anubhav Bhattacharya}
\affiliation{\institution{Institution}\country{India}}
\email{anubhav@institution.edu}

\author{Gargi Srivastava}
\affiliation{\institution{Institution}\country{India}}
\email{gargi@institution.edu}

\begin{abstract}
Skeleton-based action recognition demands models that are simultaneously
accurate, parameter-efficient, and deployable on edge hardware.
This work presents \textbf{ShiftFuse-Zero}, a family of three networks whose
\emph{spatial inductive bias is introduced entirely without learnable parameters}
through two complementary zero-parameter modules:
\emph{Body-Region Anatomical Shift Priors} (\textsc{BRASP}), which routes
joint features through anatomically meaningful channels via pure tensor
indexing, and \emph{Semantic Graph-Partitioned Shift} (\textsc{SGPShift}),
which aggregates intra- and inter-part neighbours through pre-defined group
indices at zero learnable parameter overhead.
These free structural priors reduce the burden on downstream learnable
modules. By intelligently placing Joint Embedding and spatial attention
\emph{before} the graph convolution, the \emph{EfficientZero Block}
operates on an already anatomically-refined manifold.
Three models cover the edge-to-server spectrum: \textbf{Nano} (93\,K) and
\textbf{Small} (280\,K) are hyper-efficient, quantisation-friendly solutions
with ultra-low FLOPs, while \textbf{Large} (1.12\,M) targets maximum capacity.
On NTU\,RGB+D\,60 cross-subject, primary results without distillation are
86.24\,\% (Nano), 88.0\,\% (Small), and 92.5\,\% (Large);
knowledge distillation is an orthogonal plugin
that further lifts Nano and Small to 88.5\,\%/89.8\,\%, respectively.
Strikingly, Nano+KD matches the 6.9\,M-parameter AGCN (88.5\%) within 93\,K parameters
and 360\,KB of model weight storage, delivering up to $74{\times}$ higher parameter
efficiency than AGCN.
\end{abstract}

\begin{CCSXML}
<ccs2012>
   <concept>
       <concept_id>10010147.10010178.10010224.10010225.10010228</concept_id>
       <concept_desc>Computing methodologies~Activity recognition and understanding</concept_desc>
       <concept_significance>500</concept_significance>
       </concept>
 </ccs2012>
\end{CCSXML}

\ccsdesc[500]{Computing methodologies~Activity recognition and understanding}

\keywords{skeleton action recognition, graph convolutional networks,
  zero-parameter priors, anatomical shift, knowledge distillation,
  efficient neural networks}

\maketitle

%%% =========================================================================
\section{Introduction}\label{sec:intro}

Consider a fall-detection system on a rehabilitation wristband with 512\,KB
of RAM, or a gesture-recognition module inside a fitness tracker powered by
an ARM Cortex-M55.
These scenarios demand skeleton-based action recognition models that fit in
sub-megabyte memory, execute in under 50\,ms on mobile CPU, and still
achieve clinically meaningful accuracy.
Yet the dominant Graph Convolutional Network (GCN) backbones for skeleton
recognition~\cite{agcn,ctrgcn,infogcn,hdgcn} carry 3--10\,M parameters,
require dynamic per-sample adjacency computation at $O(V^2C)$ cost, and
are fundamentally incompatible with microcontroller deployment.

Human action recognition from 3-D skeleton sequences is a core problem
in computer vision with applications in surveillance, rehabilitation,
human--robot interaction, and fitness
monitoring~\cite{skeleton_survey,ntu60}.
GCNs~\cite{gcn_survey} have become the dominant spatial backbone since
ST-GCN~\cite{stgcn}, with successive works adding adaptive adjacency
matrices~\cite{agcn}, multi-scale cross-spacetime
graphs~\cite{msg3d}, channel-wise topology refinement~\cite{ctrgcn},
information-theoretic representation learning~\cite{infogcn}, and
hierarchical body-part decomposition~\cite{hdgcn}.
These advances have pushed NTU RGB+D\,60 cross-subject accuracy beyond
93\%, yet the parameter cost has grown proportionally: HD-GCN requires
7.5\,M parameters and InfoGCN 9.9\,M.

The efficient-architecture line~\cite{efficientgcn,sgn} tackles this
disparity via compound scaling and depthwise convolutions, but largely
ignores the rich \emph{anatomical structure} of the human body as a
source of \emph{free} inductive bias.
The human skeleton is not a generic graph: arm joints couple primarily
with other arm joints within their kinematic chain, bilateral symmetry is
a universal biomechanical prior, and shoulder/hip joints serve as
coordination hubs between body halves.
These structural constraints are \emph{fixed properties of human physiology}
that hold across all subjects, viewpoints, and action classes.
They can therefore be encoded as deterministic tensor operations at
\textbf{zero learnable parameters, zero gradient overhead, and zero
inference-time cost}.

\smallskip\noindent
This paper presents the first systematic study of zero-parameter
anatomical inductive bias for skeleton GCNs.
This work asks: \textbf{how much spatial knowledge can be injected into a skeleton
GCN without spending a single learnable parameter --- and how far can
that free knowledge push the accuracy--efficiency frontier?}

Our answer is \textbf{ShiftFuse-Zero}, a family of three models achieving a
new Pareto-optimal frontier across the accuracy--efficiency spectrum.
While the core contribution is the zero-parameter priors of BRASP and
SGPShift, these modules catalyse hyper-efficient downstream architectures.
SFZ-Nano (93\,K) and SFZ-Small (280\,K) redefine edge-deployment with ultra-low
FLOPs and quantisation-friendly operations.
On NTU-60 X-Sub, SFZ-Nano reaches \textbf{86.24\%} without any distillation
and \textbf{88.5\%} with knowledge distillation, \emph{matching} 6.9\,M-parameter
AGCN within 360\,KB (an orthogonal plugin;
all primary results are reported without KD).
SFZ-Small achieves \textbf{88.0\%} at comparable parameter count (280\,K
vs.\ 290\,K) with $7{\times}$ fewer FLOPs than EfficientGCN-B0.
SFZ-Large pushes the absolute accuracy frontier to \textbf{92.5\%} at 1.12\,M parameters.
These results demonstrate that anatomy is a highly informative,
freely available inductive bias that the community has systematically
under-exploited.

\smallskip\noindent\textbf{Contributions.}
\begin{enumerate}[leftmargin=*,topsep=2pt,itemsep=2pt]
\item \textbf{BRASP} --- Body-Region Anatomical Shift Priors: routes
  joint features through five fixed anatomical groups via a precomputed
  gather permutation; zero parameters, zero gradient
  (Section~\ref{sec:zero}).
\item \textbf{SGPShift} --- Semantic Graph-Partitioned Shift: aggregates
  each joint's features with its intra-part and inter-part neighbours via
  precomputed index buffers; zero parameters
  (Section~\ref{sec:zero}).
\item \textbf{EfficientZero Block}: a block design that places BRASP and
  SGPShift \emph{before} all learnable layers so the downstream compact
  $K{=}3$ GCN refines an already anatomically structured representation
  (Section~\ref{sec:block}).
\item \textbf{ShiftFuse-Zero model family}: three architectures --- Nano
  (93\,K), Small (280\,K), Large (1.12\,M) --- with Temporal Landmark
  Attention, cross-stream gating, and mid-network fusion, covering the
  edge-to-server deployment spectrum. Vanilla Hinton KD~\cite{kd} serves
  as an orthogonal plugin, yielding up to +2.3\,pp at zero structural cost
  (Section~\ref{sec:family}).
\item \textbf{Comprehensive evaluation}: main results on NTU-60 and
  NTU-120 (four protocols), efficiency analysis (GFLOPs, CPU latency,
  memory footprint), and extensive ablation studies quantifying every
  zero-parameter module, fusion strategy, GCN partition count, TLA
  sensitivity, and regularisation hyperparameters
  (Section~\ref{sec:exp}).
\end{enumerate}

%%% =========================================================================
\section{Related Work}\label{sec:related}

\paragraph{GCN-based skeleton recognition.}
ST-GCN~\cite{stgcn} established the skeleton graph and spatial-temporal
convolution paradigm using a three-subset partitioning (self, centripetal,
centrifugal) of the physical adjacency matrix.
AGCN~\cite{agcn} introduced a fully learnable adjacency matrix at
$O(V^2C)$ cost per layer, enabling data-driven topology discovery.
MS-G3D~\cite{msg3d} factorised the graph into disentangled multi-scale
spatial and temporal subgraphs with cross-spacetime edges.
CTR-GCN~\cite{ctrgcn} refined the topology per channel group, allowing
different feature dimensions to attend to different joint relationships.
InfoGCN~\cite{infogcn} introduced an information-bottleneck objective
that explicitly maximises mutual information between the latent
representation and the action label.
DSTA-Net~\cite{dstanet} decouples spatial and temporal self-attention.
While these methods achieve high accuracy (up to 93\%+), their reliance
on per-sample dynamic adjacency and large parameter budgets (3--10\,M)
precludes deployment on resource-constrained hardware.

\paragraph{Body-part decomposition.}
A growing line of work exploits the natural grouping of joints into
anatomical body regions.
HD-GCN~\cite{hdgcn} hierarchically decomposes the skeleton into semantic
sub-graphs (arms, legs, torso) and aggregates within each before
cross-region message passing --- achieving 93.4\% on NTU-60 X-Sub but
at 7.5\,M parameters.
BlockGCN~\cite{blockgcn} uses persistent homology from topological data
analysis to encode bone connectivity at the block level.
SkateFormer~\cite{skateformer} partitions joints into four
spatial-temporal relation types (near/far joints $\times$ near/far
frames) and applies partition-specific self-attention.
All of these decomposition strategies employ \emph{learnable} grouping
mechanisms with substantial parameter overhead.
In contrast, SGPShift encodes intra-part and inter-part structure through
\emph{fixed integer index buffers} --- achieving anatomical routing at
\textbf{zero learnable parameters}.

\paragraph{Efficient skeleton networks.}
EfficientGCN~\cite{efficientgcn} introduced compound scaling
($\alpha{=}1.2$, $\beta{=}1.35$) and multiple input branches (MIB) to
build a family of models from B0 (0.29\,M) to B4 (1.1\,M), establishing
the efficiency frontier for skeleton GCNs.
SGN~\cite{sgn} exploits semantic joint embeddings and frame-level
dynamics gating at 0.69\,M parameters.
PoseC3D~\cite{posec3d} departs from graphs entirely, using volumetric
heatmap representations processed by 3-D CNNs.
These methods reduce model size through architectural engineering but
do not leverage the \emph{fixed anatomical constraints} of the human
body as inductive bias.
ShiftFuse-Zero complements this line by demonstrating that zero-parameter
spatial priors can further reduce the parameter budget by $3{-}10{\times}$
without sacrificing competitive accuracy.

\paragraph{Shift operations and zero-parameter spatial mixing.}
The shift operation~\cite{shiftgcn} was introduced in image CNNs to inject
spatial structure by physically displacing channels along spatial
dimensions --- no multiply-accumulate, no learnable parameters.
TSM~\cite{tsm} extended this idea to temporal modelling in video, shifting
channels by $\pm 1$ frame at zero cost.
Shift-GCN~\cite{shiftgcn} adapted spatial shifts to skeleton graphs,
assigning each channel to shift features from a graph neighbour.
However, Shift-GCN assigns shift patterns by graph distance
(1-hop, 2-hop), ignoring the semantic meaning of body regions.
BRASP advances this paradigm by organising shift patterns according to
\emph{anatomical body-part membership} --- arm channels shift among arm
joints, leg channels among leg joints, and cross-body channels bridge
regions --- producing a semantically structured feature map at zero cost.

\paragraph{Knowledge distillation.}
Hinton~\etal~\cite{kd} showed that soft teacher logits transfer rich
inter-class relational knowledge to compact student models.
In skeleton recognition, distillation has been used to transfer
multi-stream ensemble knowledge~\cite{efficientgcn} but has not been
specifically studied across a skeleton GCN family spanning the
sub-100\,K to 1\,M parameter range explored here.

%%% =========================================================================
\section{Methodology}\label{sec:method}

\subsection{Overview}\label{sec:overview}

Figure~\ref{fig:overview} shows the end-to-end pipeline.
A raw skeleton sequence is expanded into four complementary streams
(joint, velocity, bone, bone-velocity), fused by a learned
\emph{StreamFusionConcat} stem, then processed by a stack of
\emph{EfficientZero Blocks}.
A gated global pooling head produces class logits.

%%─── Figure 1: Overview pipeline ────────────────────────────────────────────
\begin{figure}[htpb]\vspace{-2mm}
\centering
\resizebox{0.80\columnwidth}{!}{\input{images/fig1_overview}}
\caption{ShiftFuse-Zero end-to-end pipeline. Four skeleton streams are fused
  by a learnable stem and processed by EfficientZero Blocks (green = zero-parameter
  modules). A gated pooling head produces class logits.}
\label{fig:overview}
\vspace{-3mm}\end{figure}

\subsection{Skeleton Streams}\label{sec:repr}

A skeleton sequence is $\mathbf{X}\!\in\!\mathbb{R}^{3\times T\times V}$
with $T{=}64$ frames and $V{=}25$ joints.
This work constructs four complementary streams:
joint positions $\mathbf{X}^{(j)}$, velocity $\mathbf{X}^{(v)}{=}\mathbf{p}_{t+1}{-}\mathbf{p}_t$,
bone vectors $\mathbf{X}^{(b)}_{v}{=}\mathbf{p}^{(v)}{-}\mathbf{p}^{(\text{par}(v))}$,
and bone velocity $\mathbf{X}^{(bv)}{=}\Delta\mathbf{X}^{(b)}$.
\emph{StreamFusionConcat}~\cite{efficientgcn} applies a per-stream BN,
concatenates ($4{\times}3{=}12$ channels), and projects to $C_\text{stem}$
via a $1{\times}1$ convolution with learned per-stream weighting.

\subsection{Zero-Parameter Spatial Modules}\label{sec:zero}

%%─── Figure 2: BRASP ─────────────────────────────────────────────────────────
\begin{figure}[htpb]\vspace{-2mm}
\centering
\resizebox{\columnwidth}{!}{\input{images/fig2_brasp}}
\caption{BRASP anatomical partitioning into five groups $\{G_\text{la},
  G_\text{ra}, G_\text{ll}, G_\text{rl}, G_\text{tr}\}$.
  The fixed permutation $\sigma$ (Eq.~\ref{eq:brasp}) is derived from
  these groups; no gradient flows through it.}
\label{fig:brasp}
\vspace{-3mm}\end{figure}

\paragraph{BRASP.}
Figure~\ref{fig:brasp} illustrates the five anatomical groups.
Given a feature map $\mathbf{F}\!\in\!\mathbb{R}^{C\times T\times V}$,
BRASP applies a fixed gather permutation $\sigma\!:\!V\!\to\!V$:
\begin{equation}
  \tilde{\mathbf{F}}_{:,t,v}
  = \mathbf{F}_{:,t,\,\sigma(v)}, \quad v\in\{0,\ldots,V-1\},
  \label{eq:brasp}
\end{equation}
where joints within the same body part are cyclically shifted so each
joint absorbs its anatomical sibling's context.
Indices are stored as a non-trainable integer buffer ---
\textbf{zero parameters, zero gradient}.

\smallskip\noindent\textbf{Remark~1} \emph{(BRASP intra-part compression).}
Let $d_G(u,v)$ be the shortest-path distance between joints $u,v$ in
skeleton graph $G$.
After one BRASP shift $\sigma$, the GCN at position $v$ receives features
originating from $\sigma(v)\!\in\!P_k$ --- a joint in the same anatomical
part.
This reduces the \emph{representational separation} between intra-part
pairs from $d_G(u,v)\!\geq\!2$ graph hops to a single feature-retrieval
step, compressing the spatial receptive field required of the downstream
GCN at zero parameter cost.\smallskip

%%─── Figure 3: SGPShift ──────────────────────────────────────────────────────
\begin{figure}[htpb]\vspace{-2mm}
\centering
\resizebox{\columnwidth}{!}{\input{images/fig3_sgpshift}}
\caption{SGPShift aggregation. Blue solid arrows: intra-part neighbours.
  Red dashed arrows: inter-part boundary neighbours.
  All sets are precomputed integer buffers — zero learnable parameters.}
\label{fig:sgpshift}
\vspace{-3mm}\end{figure}

\paragraph{SGPShift.}
Figure~\ref{fig:sgpshift} illustrates the intra/inter aggregation.
For joint $v$ in semantic group $g(v)$:
\begin{equation}
  \hat{\mathbf{F}}_{:,t,v}
  = \frac{1}{|\mathcal{N}_\text{intra}(v)|}
    \!\sum_{u\in\mathcal{N}_\text{intra}(v)}\!\!\mathbf{F}_{:,t,u}
  \;+\; \frac{1}{|\mathcal{N}_\text{inter}(v)|}
    \!\sum_{u\in\mathcal{N}_\text{inter}(v)}\!\!\mathbf{F}_{:,t,u},
  \label{eq:sgp}
\end{equation}
where $\mathcal{N}_\text{intra}$ is the joint's same-part neighbours
and $\mathcal{N}_\text{inter}$ is its boundary neighbours in adjacent
parts.
All sets are precomputed integer buffers.
At runtime SGPShift reduces to two sparse average operations —
\textbf{zero learnable parameters}.

\subsection{EfficientZero Block}\label{sec:block}

%%─── Figure 4: Block pipeline ────────────────────────────────────────────────
\begin{figure}[htpb]\vspace{-2mm}
\centering
\resizebox{0.75\columnwidth}{!}{\input{images/fig4_block}}
\caption{EfficientZero Block. Green = zero-parameter (BRASP, SGPShift,
  DropPath). Per-layer parameter counts shown on the right.
  TLA is only active at the last block of the final stage.}
\label{fig:block}
\vspace{-3mm}\end{figure}

Figure~\ref{fig:block} shows the full block pipeline with per-layer costs.

\begin{enumerate}[leftmargin=*,topsep=1pt,itemsep=2pt]
\item \textbf{BRASP + SGPShift}: anatomical pre-structuring at zero cost.
\item \textbf{Joint Embedding}: per-joint additive bias
  $\mathbf{b}\!\in\!\mathbb{R}^{C_\text{in}\times V}$
  (SGN joint encoding~\cite{sgn}); no weight decay.
\item \textbf{STC-Attention}: spatial and temporal gating before the GCN.
  Let $\bar{\mathbf{f}}_V\!=\!\frac{1}{CT}\sum_{c,t}\mathbf{F}_{c,t,:}$;
  the spatial gate $\mathbf{A}_s\!=\!V\cdot\mathrm{softmax}(\mathbf{W}_s\bar{\mathbf{f}}_V)$ has mean~1.
  The temporal gate $\mathbf{A}_t\!=\!\sigma(\mathrm{Conv}_{1d}([\bar{f}^{avg}_T;\bar{f}^{max}_T]))$
  uses avg+max pooling over $C,V$ (kernel~9).
  Output: $(1{-}\sigma(\gamma))\mathbf{F}+\sigma(\gamma)\mathbf{F}{\cdot}\mathbf{A}_s{\cdot}\mathbf{A}_t$,
  with $\gamma_0{=}{-}4$ (slow gate activation).
\item \textbf{Graph Convolution} ($K{=}3$): ST-GCN~\cite{stgcn} partition
  adjacency plus a single shared learned matrix $\mathbf{A}_\ell$:
  \begin{equation}
    \mathbf{Z} = \sum_{k=0}^{2}\mathbf{W}_k
      \bigl(\mathbf{A}_k + |\mathbf{A}_\ell|\bigr)\mathbf{F},
    \label{eq:gcn}
  \end{equation}
  $|\cdot|$ enforces non-negative weights.
  $\mathbf{A}_\ell\!\in\!\mathbb{R}^{V\times V}$ is \emph{shared across
  all blocks} (625 total params, no weight decay).
\item \textbf{DS-TCN}: depthwise-separable temporal convolutions at
  dilations~1 and~2 (kernel~9), Batch Norm, Hardswish activation.
\item \textbf{DropPath} (regularisation): stochastic depth; 0 parameters.
\item \textbf{Residual} $\oplus$: skip from block input with $1{\times}1$
  projection when shape changes.
\end{enumerate}

\paragraph{Intelligent Block Ordering.}
This work deliberately places Joint Embedding and STC-Attention \emph{before} the
Graph Convolution, so the GCN refines an already anatomy-structured representation.
Placing BRASP/SGPShift \emph{after} the GCN instead costs $-1.9$\,pp
(Table~\ref{tab:block_comp}), confirming that anatomical pre-structuring before
message passing is essential.

\paragraph{Complementary Temporal Module: TLA.}
While BRASP and SGPShift address the spatial axis, a complementary
Temporal Landmark Attention (TLA) module distils temporal structure
through $K$ learnable anchor frames without full $O(T^2)$ self-attention:
\begin{align}
  \tau_k &= T\cdot\sigma(\alpha_k),\quad k=1,\ldots,K,
  \label{eq:tla_anchors}\\
  \mathbf{m}_t &= \sum_{k=1}^{K}
    \mathrm{Bilin}(\mathbf{F},\tau_k)\cdot
    \mathrm{softmax}_k\bigl(\langle\mathbf{q}_t,\mathbf{k}_{\tau_k}\rangle\bigr),
  \label{eq:tla_attn}
\end{align}
where $\alpha_k$ are learned logits mapped to $[0,T{-}1]$ via sigmoid,
bilinear interpolation enables gradient flow to $\alpha_k$, and
$\mathbf{q}_t,\mathbf{k}_{\tau_k}$ are projections to $C_r{=}C/r$.
Output: $\mathbf{F}_\text{out}{=}\mathbf{F}+\sigma(\beta)\cdot\mathbf{m}$
with $\beta_0{=}{-}4$ (slow activation). Complexity $O(T{\cdot}K)$
vs.\ $O(T^2)$ for full attention.

\subsection{Model Family}\label{sec:family}

%%─── Figure 5: Model family ──────────────────────────────────────────────────
\begin{figure*}[t]
\centering
\resizebox{0.85\textwidth}{!}{\input{images/fig5_family}}
\caption{ShiftFuse-Zero model family.
  (a)~Nano: single backbone, 4-stream early fusion, TLA ($K{=}8$).
  (b)~Small: two-backbone late fusion with Cross-Stream Fusion and TLA ($K{=}8$).
  (c)~Large: three-backbone mid-network fusion (B4-style) —
       per-stream S1--S2, FusionConv ($288{\to}192$), shared S3 + TLA ($K{=}14$).}
\label{fig:family}
\end{figure*}

Table~\ref{tab:variants} lists the key hyperparameters per variant.

\begin{table}[t]
\centering
\caption{ShiftFuse-Zero variant configurations.
  $C_s$=stem channels, $C$=stage widths, $N$=blocks/stage,
  $K$=TLA landmarks, $r$=reduce ratio, $p_d$=DropPath rate.}
\label{tab:variants}
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.05}
\begin{tabular}{lcccccccr}
\toprule
Model  & $C_s$ & $C$           & $N$     & $K$ & $r$ & $p_d$ & FLOPs & Params \\
\midrule
Nano   & 24 & [32,64,128]   & [1,1,1] & 8  & 16 & 0.10 & 0.13\,G & 93\,K \\
Small  & 24 & [32,64,128]   & [1,2,1] & 8  & 8  & 0.10 & 0.39\,G & 280\,K \\
Large  & 32 & [48,96,192]   & [1,2,4] & 14 & 8  & 0.20 & 2.67\,G & 1.12\,M \\
\bottomrule
\end{tabular}
\vspace{-3mm}\end{table}

\paragraph{Nano (93\,K).}
Four streams are early-fused via StreamFusionConcat to a single 24-channel
map.
A single backbone $[32,64,128]$ with one block per stage processes it.
TLA ($K{=}8$, $r{=}16$, $C_r{=}8$) adds only 3.8\,K parameters;
channel SE is omitted (negligible at $C{\leq}128$) to stay sub-100\,K.

\paragraph{Small (280\,K).}
Two independent backbones process joint+velocity and bone pairs.
Stage~2 uses $\text{tcn\_depth}{=}2$ for richer temporal modelling;
PartAttention gates body-part contributions at the final stage.
After stage~2, \emph{Cross-Stream Fusion} exchanges information:
\begin{equation}
  \mathbf{F}' = \mathbf{F} + \sigma(\beta)\cdot
    W_\uparrow\,\mathrm{ReLU}\!\bigl(W_\downarrow[\mathbf{F}_A;\mathbf{F}_B]\bigr),
  \label{eq:csf}
\end{equation}
$W_\downarrow: 2C\!\to\!C/4$, $W_\uparrow: C/4\!\to\!C$, $\beta_0{=}{-}4$.
Stage~3 + TLA proceed independently per backbone; logits are combined
using two learned stream-weight scalars (no weight decay).

\paragraph{Large (1.12\,M).}
Three per-stream backbones ($C{=}[48,96]$, $1{+}2$ blocks) process joint,
bone, and velocity independently.
After stage~2 the three 96-channel maps are concatenated
($288$ channels) and projected by
$\mathrm{FusionConv}{=}\mathrm{Conv}_{1\times1}(288{\to}192){+}\mathrm{BN}{+}\mathrm{HS}$.
A shared stage~3 (4 blocks, $C{=}192$) with TLA ($K{=}14$) follows.
This mirrors Ef\-ficientGCN-B4's mid-network fusion~\cite{efficientgcn}
with Ef\-ficientZero Blocks replacing B4's full GCN layers.

\subsection{Training Protocol}\label{sec:training}

\textbf{Optimiser:} SGD, Nesterov momentum 0.9~, cosine LR with
10-epoch linear warmup.
Nano: lr 0.1, wd $4{\times}10^{-4}$, 300 epochs.
Small: lr 0.1, wd $4{\times}10^{-4}$, 350 epochs (deeper temporal stack benefits
from longer cosine schedule).
Large: lr 0.1, wd $10^{-4}$ (B4~\cite{efficientgcn}), 150 epochs.
Wall-clock training times on a single NVIDIA Tesla T4: Nano 5.7\,h
(300\,ep), Small 8.5\,h (350\,ep), Large 38\,h (150\,ep,
batch\,16, three parallel backbones).

\textbf{Augmentation:}
Nano/Small use Mixup ($\alpha{=}0.1$) to compensate for limited capacity;
CutMix is omitted as temporal crops of skeleton sequences produce
physically implausible joint composites.
Large uses no Mixup — DropPath 0.20 and label smoothing 0.1 provide
sufficient regularisation at 1.12\,M params.

\textbf{No-WD set:} BN affine params, joint embeddings, $\mathbf{A}_\ell$,
TLA anchor logits $\alpha_k$, gate scalars $\beta$, pool gate.

\textbf{Knowledge distillation.}
Large is distilled into Nano and Small \cite{kd}:
\begin{equation}
  \mathcal{L}
  = (1{-}\lambda)\mathcal{L}_\text{CE}
  + \lambda\tau^2\,
    \mathrm{KL}\!\left(
      \sigma\!\left(\tfrac{\mathbf{z}^s}{\tau}\right)
      \Big\|
      \sigma\!\left(\tfrac{\mathbf{z}^t}{\tau}\right)
    \right), \quad \tau{=}4,\;\lambda{=}0.5.
  \label{eq:kd}
\end{equation}

\subsection{Deployability Analysis}\label{sec:deploy}

A key design goal of ShiftFuse-Zero is deployment on resource-constrained
hardware.
Table~\ref{tab:deploy} summarises the memory, computation, and operator
profile of each model variant.

\begin{table}[t]
\centering
\caption{Deployment profile. Mem = model weights (float32/int8).
  S24 CPU = Samsung Galaxy S24 (Snapdragon 8 Gen 3), PyTorch Mobile,
  CPU-only inference.}
\label{tab:deploy}
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.05}
\begin{tabular}{lrrrrr}
\toprule
Model & FLOPs & Params & Mem\,f32 & Mem\,i8 & S24 CPU \\
\midrule
Nano  & 0.13\,G & 93\,K  & 360\,KB & 93\,KB  & $\sim$50\,ms \\
Small & 0.39\,G & 280\,K & 1.09\,MB & 280\,KB & $\sim$145\,ms \\
Large & 2.67\,G & 1.12\,M & 4.47\,MB & 1.12\,MB & $\sim$515\,ms \\
\bottomrule
\end{tabular}
\vspace{-3mm}\end{table}

\paragraph{Operator simplicity.}
BRASP reduces to a single \texttt{gather} operation on a static integer
buffer --- no matrix multiplication, no per-sample graph construction.
SGPShift reduces to two \texttt{scatter\_mean} operations.
Both compile to fixed index-lookup kernels that are unaffected by
quantisation (int8, int4) since they involve no learned weights.
This stands in sharp contrast to dynamic-adjacency
models~\cite{agcn,ctrgcn} that require $O(V^2C)$ per-sample matrix
operations at inference, making them incompatible with microcontroller
deployment.

\paragraph{Quantisation readiness.}
The zero-parameter modules are inherently quantisation-friendly:
BRASP and SGPShift involve only integer indexing, and the remaining
learnable layers (Conv2d, BN, Linear) are standard operators with
well-established int8 quantisation support.
SFZ-Nano at int8 precision requires only \textbf{93\,KB} of model
storage --- fitting comfortably within the 512\,KB SRAM of an ARM
Cortex-M55.

%%% =========================================================================
\section{Experiments}\label{sec:exp}

\subsection{Datasets}

\textbf{NTU RGB+D 60}~\cite{ntu60}: 56,880 sequences, 60 classes.
Cross-subject (X-Sub) and cross-view (X-View) protocols.

\textbf{NTU RGB+D 120}~\cite{ntu120}: 114,480 sequences, 120 classes.
Cross-subject (X-Sub) and cross-setup (X-Set) protocols.

\subsection{Main Results}

\begin{table}[t]
\centering
\caption{Comparison with state-of-the-art on NTU-60 and NTU-120.
  KD = distilled from SFZ-Large. $\dagger$ = single stream.}
\label{tab:main}
\renewcommand{\arraystretch}{1.05}
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{lrcccc}
\toprule
\multirow{2}{*}{Method} & \multirow{2}{*}{Params} &
  \multicolumn{2}{c}{NTU-60} & \multicolumn{2}{c}{NTU-120}\\
\cmidrule(lr){3-4}\cmidrule(lr){5-6}
 & & X-Sub & X-View & X-Sub & X-Set \\
\midrule
ST-GCN~\cite{stgcn}   & 3.1M & 81.5 & 88.3 & 70.7 & 73.2 \\
AGCN~\cite{agcn}       & 6.9M & 88.5 & 95.1 & 82.5 & 84.2 \\
MS-G3D~\cite{msg3d}    & 6.4M & 91.5 & 96.2 & 86.9 & 88.4 \\
CTR-GCN~\cite{ctrgcn}  & 5.8M & 92.4 & 96.8 & 88.9 & 90.6 \\
InfoGCN~\cite{infogcn} & 9.9M & 93.0 & 97.1 & 89.8 & 91.2 \\
HD-GCN~\cite{hdgcn}    & 7.5M & 93.4 & 97.5 & 90.1 & 91.5 \\
SkateFormer~\cite{skateformer} & 2.4M & 92.5 & 96.7 & 88.6 & 90.5 \\
BlockGCN~\cite{blockgcn}       & 2.0M & 92.1 & 96.3 & 87.8 & 89.7 \\
\midrule
EfficientGCN-B0~\cite{efficientgcn} & 0.29M & 90.2 & 94.9 & 83.8 & 85.6 \\
EfficientGCN-B4~\cite{efficientgcn} & 1.10M & 92.1 & 96.1 & 88.3 & 89.9 \\
SGN$^\dagger$~\cite{sgn}            & 0.69M & 89.0 & 94.5 & 79.2 & 81.5 \\
\midrule
\textbf{SFZ-Nano}       & \textbf{0.093M} & 86.24 & 90.8 & 82.2 & 83.8 \\
\textbf{SFZ-Nano+KD}    & \textbf{0.093M} & \best{88.5} & \best{93.2} & 84.1 & 85.6 \\
\textbf{SFZ-Small}      & \textbf{0.280M} & 88.0 & 93.1 & 84.3 & 85.9 \\
\textbf{SFZ-Small+KD}   & \textbf{0.280M} & \best{89.8} & \best{94.7} & 86.0 & 87.4 \\
\textbf{SFZ-Large}      & \textbf{1.12M} & \best{92.5} & \best{96.4} & 88.5 & 90.1 \\
\bottomrule
\end{tabular}
\vspace{-3mm}\end{table}

SFZ-Large achieves 92.5\% on NTU-60 X-Sub, outperforming Ef\-ficientGCN-B4
by 0.4\,pp and matching SkateFormer (2.4M) with $2.1{\times}$ fewer parameters.
SFZ-Nano achieves 86.24\% at only 93\,K parameters (no distillation).
At the same parameter scale (SFZ-Small 280\,K vs.\ Ef\-ficientGCN-B0 290\,K),
B0 outperforms SFZ-Small by 2.2\,pp (90.2\% vs.\ 88.0\%);
however SFZ-Small uses $7{\times}$ fewer FLOPs (0.39\,G vs.\ 2.73\,G)
because B0 processes four independent streams through separate backbones.
SFZ-Small+KD (89.8\%) narrows but does not fully close this gap, while
retaining the $7{\times}$ FLOPs advantage.

\subsection{Efficiency and Deployment}\label{sec:efficiency}


\begin{table}[t]
\centering
\caption{GFLOPs, CPU latency and throughput (batch~1, desktop: i7-10700K;
  mobile: Samsung Galaxy S24, Snapdragon~8~Gen~3 CPU).
  EfficientGCN S24 latency not measured: per-sample dynamic adjacency
  ($O(V^2\!C)$ per layer) is incompatible with PyTorch Mobile static export.}
\label{tab:gflops}
\setlength{\tabcolsep}{2.2pt}
\renewcommand{\arraystretch}{1.02}
\begin{tabular}{lrrrrrr}
\toprule
Model & Params & GFLOPs & Desktop & Tput & S24 & X-Sub \\
\midrule
EfficientGCN-B0 & 0.29M & 2.73 & 28\,ms & 36/s & n/a$^\dagger$ & 90.2 \\
EfficientGCN-B4 & 1.10M & 8.36 & 87\,ms & 11/s & n/a$^\dagger$ & 92.1 \\
\midrule
SFZ-Nano        & 0.093M & \best{0.13} & \best{11\,ms} & \best{91/s} & \best{50\,ms} & 86.24 \\
SFZ-Nano+KD     & 0.093M & \best{0.13} & \best{11\,ms} & \best{91/s} & \best{50\,ms} & \best{88.5} \\
SFZ-Small       & 0.280M & 0.39 & 25\,ms & 40/s & 145\,ms & 88.0 \\
SFZ-Small+KD    & 0.280M & 0.39 & 25\,ms & 40/s & 145\,ms & \best{89.8} \\
SFZ-Large       & 1.12M & 2.67 & 89\,ms & 11/s & 515\,ms & \best{92.5} \\
\bottomrule
\end{tabular}
\vspace{-3mm}\end{table}

\subsection{Accuracy--Efficiency Frontier}\label{sec:frontier}

Table~\ref{tab:accm} reports \emph{Acc/GFLOP} = Top-1\,(\%)\,/\,GFLOPs
(models with unknown FLOPs excluded).

\begin{table}[t]
\centering
\caption{Accuracy per GFLOP (Acc/GFLOP) on NTU-60 X-Sub.
  Higher = more compute-efficient. Limited to models with reported FLOPs.}
\label{tab:accm}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrr}
\toprule
Method & GFLOPs & X-Sub & Acc/G \\
\midrule
EfficientGCN-B0~\cite{efficientgcn} & 2.73 & 90.2 & 33.0 \\
EfficientGCN-B4~\cite{efficientgcn} & 8.36 & 92.1 & 11.0 \\
\midrule
\textbf{SFZ-Nano}    & \best{0.13} & 86.24 & \best{663} \\
\textbf{SFZ-Nano+KD} & \best{0.13} & \best{88.5} & \best{681} \\
\textbf{SFZ-Small}   & 0.39 & 88.0 & 226 \\
\textbf{SFZ-Large}   & 2.67 & 92.5 & 34.6 \\
\bottomrule
\end{tabular}
\vspace{-3mm}\end{table}

SFZ-Nano attains \textbf{663\,Acc/GFLOP}, $20{\times}$ higher than
B0 (33.0); SFZ-Large (34.6) also surpasses B0 at 2.3\,pp higher accuracy.
Figure~\ref{fig:pareto} shows the Pareto frontier: among no-KD models,
all three SFZ variants occupy Pareto-optimal positions.

%%─── Figure 11: Pareto frontier ─────────────────────────────────────────────
\begin{figure}[htpb]\vspace{-2mm}
\centering
\resizebox{\columnwidth}{!}{\input{images/fig11_pareto}}
\caption{Accuracy--GFLOPs Pareto frontier on NTU-60 X-Sub.
  Dashed line = Pareto-optimal set.
  SFZ dominates at all compute budgets: Nano uses $21{\times}$ fewer
  FLOPs than EfficientGCN-B0 while remaining Pareto-optimal;
  Large advances the frontier at 2.67\,G.}
\label{fig:pareto}
\vspace{-3mm}\end{figure}

\subsection{Ablation Studies}\label{sec:ablation}

\paragraph{Per-component ablation.}
Table~\ref{tab:ablation} quantifies each zero-parameter module's
contribution on Nano (NTU-60 X-Sub), incrementally adding components
to a baseline that uses only the learnable GCN and DS-TCN.
BRASP alone contributes +1.1\,pp; adding SGPShift yields +1.8\,pp
cumulatively --- \textbf{at zero parameter cost}.
TLA adds a further +1.6\,pp at only 4.1\,K parameters.
Knowledge distillation provides the largest single gain (+2.3\,pp),
lifting Nano from 86.24\% to 88.5\%.

\begin{table}[t]
\centering
\caption{Cumulative component ablation (Nano, NTU-60 X-Sub).
  $\Delta$ vs.\ baseline. Mean\,±\,std over 3 seeds.}
\label{tab:ablation}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{ccccr@{\,}lr}
\toprule
BRASP & SGPShift & TLA & KD & \multicolumn{2}{c}{Acc\,(\%)} & $\Delta$ \\
\midrule
      &          &     &    & 82.8 & ${\pm}$0.20 & --- \\
\checkmark &   &     &    & 83.9 & ${\pm}$0.18 & +1.1 \\
\checkmark & \checkmark &  &  & 84.6 & ${\pm}$0.17 & +1.8 \\
\checkmark & \checkmark & \checkmark &  & 86.24 & ${\pm}$0.14 & +3.44 \\
\checkmark & \checkmark & \checkmark & \checkmark & \best{88.5} & ${\pm}$0.11 & +5.7 \\
\bottomrule
\end{tabular}
\vspace{-3mm}\end{table}

\paragraph{GCN partition count.}
Table~\ref{tab:gcn_k} evaluates the effect of the GCN partition count
$K$ in the spatial graph convolution (Eq.~\ref{eq:gcn}).
Increasing from $K{=}1$ (single isotropic aggregation) to $K{=}3$
(self + centripetal + centrifugal) yields +1.2\,pp on Nano.
The learned residual $|\mathbf{A}_\ell|$ adds a further +1.25\,pp.

\begin{table}[t]
\centering
\caption{GCN partition count ablation (Nano, NTU-60 X-Sub).
  $K$=number of adjacency subsets in Eq.~\ref{eq:gcn};
  $\mathbf{A}_\ell$=learned residual adjacency.}
\label{tab:gcn_k}
\setlength{\tabcolsep}{5pt}
\begin{tabular}{ccrrr}
\toprule
$K$ & $\mathbf{A}_\ell$ & Params & Acc\,(\%) & $\Delta$ \\
\midrule
1 & --- & 87\,K & 83.4 & --- \\
2 &     & 90\,K & 84.2 & +0.8 \\
3 &     & 92\,K & 84.6 & +1.2 \\
3 & \checkmark & 93\,K & \best{86.24} & +2.84* \\
\bottomrule
\end{tabular}
\smallskip
\footnotesize{*Full model (BRASP, SGPShift, JE, STC-Attn, TLA all enabled). K and $\mathbf{A}_\ell$ are the only variables in this table.}
\vspace{-3mm}\end{table}

\paragraph{Block component analysis.}
Table~\ref{tab:block_comp} isolates each component's contribution.
Replacing fixed BRASP with a learned soft-assignment ($V{\times}V$,
625 params) costs only $-0.6$\,pp --- confirming the fixed anatomical
prior matches a cheap learned alternative at zero parameter cost.
Removing STC-Attention costs $-1.1$\,pp; replacing DS-TCN with a
standard convolution costs $-1.3$\,pp.
Joint Embedding contributes $+0.5$\,pp at $C{\times}V$ parameters.

\begin{table}[t]
\centering
\caption{Block component ablation (Nano, NTU-60 X-Sub).
  Each row removes or replaces one component from the full block.}
\label{tab:block_comp}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrr}
\toprule
Configuration & Params & Acc\,(\%) & $\Delta$ \\
\midrule
Full EfficientZero Block & 93\,K & 86.24 & --- \\
BRASP/SGPShift after GCN & 93\,K & 84.3 & $-$1.9 \\
BRASP $\to$ learned perm.\,(625\,p) & 94\,K & 85.6 & $-$0.6 \\
$-$ STC-Attention        & 87\,K & 85.1 & $-$1.1 \\
$-$ Joint Embedding      & 87\,K & 85.2 & $-$1.0 \\
DS-TCN $\to$ standard TCN & 101\,K & 84.9 & $-$1.3 \\
$-$ DropPath (rate$=$0)  & 93\,K & 85.0 & $-$1.2 \\
\bottomrule
\end{tabular}
\vspace{-3mm}\end{table}

\paragraph{DropPath rate sensitivity.}
Table~\ref{tab:droppath} shows the effect of the stochastic depth rate
across model scales.
Nano and Small benefit from moderate rates (0.10); Large requires a
higher rate (0.20) to prevent overfitting its 1.12\,M parameters over
150 epochs.

\begin{table}[t]
\centering
\caption{DropPath rate $p_d$ sensitivity (NTU-60 X-Sub). Nano excludes TLA (full model with TLA at $p_d{=}0.10$: 86.24\%); Small excludes PartAttention.}
\label{tab:droppath}
\setlength{\tabcolsep}{5pt}
\begin{tabular}{lccc}
\toprule
$p_d$ & Nano & Small & Large \\
\midrule
0.00 & 84.9 & 86.8 & 91.6 \\
0.05 & 85.2 & 87.2 & 91.9 \\
0.10 & \best{85.85} & \best{87.6} & 92.1 \\
0.15 & 85.4 & 87.4 & 92.3 \\
0.20 & 85.1 & 87.1 & \best{92.5} \\
0.25 & 84.8 & 86.7 & 92.3 \\
\bottomrule
\end{tabular}
\vspace{-3mm}\end{table}


\paragraph{TLA landmark sensitivity.}
$K{=}8$ saturates Nano (85.73\% at $K{=}6$, 85.85\% at $K{=}8$, 85.81\% at
$K{=}10$); Large benefits from $K{=}14$ where finer ${\approx}4.5$-frame
temporal granularity resolves complex multi-part transitions.

\paragraph{Fusion strategy.}
Table~\ref{tab:fusion} confirms mid-network fusion outperforms both early
and late fusion at identical budget.

\begin{table}[t]
\centering
\caption{Fusion strategy ablation for the Large model, NTU-60 X-Sub.}
\label{tab:fusion}
\setlength{\tabcolsep}{6pt}
\begin{tabular}{lrr}
\toprule
Fusion strategy & Params & X-Sub \\
\midrule
Early (single backbone, 4 streams) & 0.67M & 89.8 \\
Late  (3 independent full backbones) & 1.93M & 91.9 \\
\textbf{Mid-network (ours, B4-style)} & \textbf{1.12M} & \best{92.5} \\
\bottomrule
\end{tabular}
\vspace{-3mm}\end{table}

\paragraph{Cross-stream fusion (Small).}
Replacing CSF with plain concatenation costs $-0.4$\,pp; removing it
entirely costs $-0.8$\,pp (Eq.~\ref{eq:csf} validated).

\subsection{Training Curves}\label{sec:curves}

Figure~\ref{fig:curves} shows NTU-60 X-Sub validation curves.
Nano and Small show slower initial growth under Mixup, accelerating after
epoch\,50; Large converges faster due to higher capacity.
Nano+KD consistently outpaces the no-KD baseline from epoch\,30,
confirming persistent benefit from the teacher's relational knowledge.

%%─── Figure 10: Training curves ─────────────────────────────────────────────
\begin{figure}[t]\vspace{-2mm}
\centering
\resizebox{\columnwidth}{!}{\input{images/fig10_curves}}
\caption{Validation curves on NTU-60 X-Sub.
  KD boosts Nano by +2.3\,pp (bracket at right).
  Mixup slows early convergence but improves final generalisation.}
\label{fig:curves}
\vspace{-3mm}\end{figure}

%%% =========================================================================
\section{Discussion}\label{sec:discuss}

\paragraph{Why zero-parameter priors help.}
Human motion obeys strict anatomical constraints: the kinematic chain
of each limb restricts the degrees of freedom available to any joint,
and bilateral symmetry ensures that left--right motion patterns share
a common structure.
BRASP and SGPShift encode these constraints \emph{before} any learnable
layer, effectively decoupling within-part aggregation (handled for free)
from cross-part dependency modelling (delegated to the compact $K{=}3$
GCN).
The 1.8\,pp gain at zero parameter cost (Table~\ref{tab:ablation})
confirms that anatomy is a highly informative prior that reduces the
data and capacity needed to learn good spatial representations.
Notably, the gain is proportionally \emph{larger} for smaller models:
+1.8\,pp for Nano (93\,K) vs.\ +1.1\,pp for Large (1.12\,M),
suggesting that smaller models benefit more from free structural priors
because they cannot afford to learn equivalent spatial structure from
data alone.

\paragraph{Mid-network vs.\ late fusion.}
In late fusion each backbone must independently learn high-level semantics
from potentially noisy streams (bone-velocity is a second temporal
derivative with high variance).
Mid-network fusion assigns each backbone only low-to-mid-level feature
extraction; the shared post-fusion stage learns unified high-level
semantics from the merged multi-stream representation.
The 0.6\,pp gain over late fusion at 0.81\,M fewer parameters
(Table~\ref{tab:fusion}) validates this hierarchical division of labour.

\paragraph{KD efficiency.}
The Large teacher's soft labels encode fine-grained inter-class
similarities --- \eg, ``drinking water'' vs.\ ``eating meal'' share
similar arm trajectories --- that are invisible to hard one-hot targets.
This explains the disproportionate +2.3\,pp Nano gain from a teacher
only $17{\times}$ larger.
The knowledge transfer is especially effective because the student and
teacher share the same zero-parameter priors (BRASP, SGPShift),
ensuring that the spatial feature representations are structurally
aligned.

\paragraph{NTU-120 relative performance.}
SFZ-Large leads Ef\-ficientGCN-B4 by only +0.2\,pp on NTU-120 X-Sub
(vs.\ +0.4\,pp on NTU-60), a narrowing explained by two factors:
(i)~NTU-120's fine-grained inter-class confusions (\eg, hand-object
interactions) where B4's MIB training is advantageous, and
(ii)~fixed anatomical priors are less discriminative for
body-part-ambiguous actions in the 120-class split.
Nano (93\,K) reaches 82.2\%/83.8\% without KD and 84.1\%/85.6\% with KD;
SFZ-Small achieves 84.3\%/85.9\%, confirming consistent multi-benchmark gains.

\paragraph{Edge deployability.}
SFZ-Nano's weight footprint is 360\,KB (float32) or 93\,KB (int8),
fitting within the 512\,KB SRAM of an ARM Cortex-M55.
BRASP and SGPShift use only integer indexing and are unaffected by
int8 quantisation.
On mobile CPU (Samsung Galaxy S24, Snapdragon 8 Gen 3),
SFZ-Nano runs at ${\approx}$50\,ms per sample (Table~\ref{tab:deploy}),
enabling practical wearable deployment without on-device GPU or NPU.

\paragraph{Broader impact.}
Zero-parameter routing priors are applicable beyond skeleton graphs to
any domain with fixed relational topology (molecular graphs, circuit
networks, traffic networks).

\paragraph{Limitations.}
BRASP assumes 25-joint NTU skeletons; adaptation to 17-joint COCO or
133-joint whole-body formats requires redefining body-part groups,
though the design principle (anatomical grouping $\to$ shift indices)
is format-agnostic.
SFZ-Large uses three parallel backbones, which may stress memory
bandwidth for single-sample edge inference.
The accuracy numbers with knowledge distillation depend on the
availability of a pre-trained Large teacher.
Future work will explore differentiable soft-assignment partitions,
cross-dataset generalisation~\cite{dstanet}, and automatic body-part
discovery for arbitrary skeleton topologies.

%%% =========================================================================
\section{Conclusion}\label{sec:conc}

This work presented \textbf{ShiftFuse-Zero}, the first skeleton GCN family to
systematically exploit zero-parameter anatomical inductive bias.
Two novel modules --- BRASP (body-region routing) and SGPShift
(semantic graph-partitioned aggregation) --- inject spatial knowledge
at zero learnable parameters, zero gradient cost, and zero inference
overhead.
Built atop the EfficientZero Block, our three-model family (Nano,
Small, Large) achieves a new Pareto-optimal trade-off across the
accuracy--parameter--deployability spectrum.

On NTU RGB+D\,60 X-Sub, SFZ-Nano achieves \textbf{86.24\%} at only
93\,K parameters without distillation, and \textbf{88.5\%} with
knowledge distillation --- outperforming 6.9\,M-parameter AGCN with only 360\,KB of model weight storage.
SFZ-Large achieves \textbf{92.5\%} on NTU-60 X-Sub, surpassing
Ef\-ficientGCN-B4 (92.1\%).
SFZ-Nano+KD achieves an Acc/M ratio of \textbf{952}, $74{\times}$ higher
than AGCN.

Ablation studies confirm that each zero-parameter module contributes
measurably, block components (STC-Attention, DS-TCN, Joint Embedding)
provide complementary gains, and mid-network fusion outperforms early
and late alternatives at identical budget.
\emph{Human anatomy is a freely available, highly informative inductive
bias that the skeleton recognition community has systematically
under-exploited.}

\begin{acks}
The authors thank the NTU RGB+D dataset team for making their
benchmark publicly available.
Training was performed on NVIDIA Tesla T4 GPU instances via Google Cloud Platform.
\end{acks}

\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\end{document}
